machine:
  services:
    - docker

test:
  pre:
    - docker-compose up -d
  override:
    # - docker build -t test .
    # - docker run -d -p 80:80 test
    - curl --retry 2 --retry-delay 5 -v http://localhost:5000
  post:
    - docker-compose rm -f
